import { LoadingContainer, Title, LoadingBar } from "./loading.styles";

function Loading() {
  return (
    <LoadingContainer>
      <Title>Loading...</Title>
      <LoadingBar />
    </LoadingContainer>
  );
}

export default Loading;
